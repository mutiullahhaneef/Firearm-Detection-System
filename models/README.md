# Place your trained model weights here.
# File: models/best.pt
#
# This directory is intentionally excluded from git (.gitignore)
# because model files are large binaries (> 6 MB).
#
# Options to obtain weights:
#   1. Train your own:  python train.py
#   2. Export a model:  python export.py --format onnx
#   3. Use Git LFS:     git lfs track "models/*.pt"
#
# If you use Git LFS, run:
#   git lfs install
#   git lfs track "models/*.pt"
#   git add .gitattributes
